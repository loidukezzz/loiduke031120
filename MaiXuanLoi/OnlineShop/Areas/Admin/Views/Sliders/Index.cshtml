@model IEnumerable<MyLibrary.Model.Slider>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Danh sách thanh trượt</h2>

<p>
    <button type="button" class="btn btn-outline-primary rounded-pill mb-3">
        <a href="@Url.Action("Create", "Sliders")"><i class="fas fa-plus mr-2"></i>Thêm Mới</a>
    </button>
    <button type="button" class="btn btn-outline-primary rounded-pill mb-3">
        <a href="@Url.Action("Trash", "Sliders")"><i class="fas fa-trash mr-2"></i>Thùng Rác</a>
    </button>
</p>
<table class="table table-bordered">
    <tr>
        <th scope="col">
            #
        </th>
        <th scope="col">
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th scope="col">
            @Html.DisplayNameFor(model => model.Link)
        </th>
        <th scope="col">
            @Html.DisplayNameFor(model => model.Position)
        </th>
        <th scope="col">
            @Html.DisplayNameFor(model => model.Img)
        </th>
        @*<th scope="col">
                @Html.DisplayNameFor(model => model.Orders)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Created_at)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Created_by)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Updated_at)
            </th>
            <th scope="col">
                @Html.DisplayNameFor(model => model.Updated_by)
            </th>*@
        <th scope="col">
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th scope="col"></th>
    </tr>

    @foreach (var item in Model)
    {
    <tr>
        <td>
            <input type="checkbox" />
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Link)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Position)
        </td>
        <td>
            <img style="max-width:30%; max-height:30%;" src="~/@Html.DisplayFor(modelItem => item.Img)" />
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Orders)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Created_at)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Created_by)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Updated_at)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Updated_by)
        </td>*@
        <td>
            @if (item.Status == 1)
            {
                <a href="@Html.DisplayFor(modelItem => item.Status)" class="badge_active">Active</a>

            }
            else
            {
                <a href="@Html.DisplayFor(modelItem => item.Status)" class="badge_active3">Closed</a>
            }
        </td>
        <td>
            <div class="action_btns d-flex">
                @if (item.Status == 1)
                {
                    <a class="action_btn mr_10" href="@Url.Action("Status", "Products",new { id = item.Id })"><i class="fas fa-toggle-on"></i>_</a>
                }
                else
                {
                    <a class="action_btn mr_10" href="@Url.Action("Status", "Products",new { id = item.Id })"><i class="fas fa-toggle-off"></i>_</a>
                }
                <a href="@Url.Action("Edit", "Sliders", new { id = item.Id })" class="action_btn mr_10"> <i class="far fa-edit"> </i>_</a>
                <a href="@Url.Action("Deltrash", "Sliders", new { id = item.Id })" class="action_btn"> <i class="fas fa-trash"> </i>_</a>
                <a href="@Url.Action("Details", "Sliders", new { id = item.Id })" class="action_btn ml-10"> <i class="far fa-eye"> </i>_</a>
            </div>
        </td>
    </tr>
    }

</table>
